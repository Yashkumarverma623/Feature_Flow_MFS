{
  "name": "featureflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx vitest run tests/unit/rollout.test.ts",
    "test:api": "npx vitest run tests/api/api.test.ts"
  },
  "devDependencies": {
    "@types/supertest": "^6.0.3",
    "supertest": "^6.3.4",
    "vitest": "^1.5.0"
  }
}
